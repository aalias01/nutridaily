{
  "name": "nutrichat",
  "private": true,
  "description": "NutriChat food diary (static)",
  "scripts": {
    "build": "node scripts/inject-client-id.js",
    "test": "node tests/test-core.js && node tests/test-parse.js"
  }
}
